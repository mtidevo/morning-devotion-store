{% comment %} Main page template section {% endcomment %}
<section class="page-section">
  <div class="wrap page-content">
    <span class="rule">{{ page.title }}</span>
    <h1 class="mb-6">{{ page.title }}</h1>
    <div class="rte">
      {{ page.content }}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Main page",
  "settings": []
}
{% endschema %}
